# Qwen Code Desktop API

This site is generated from XML documentation comments and project metadata

## Projects

- `QwenCode.Core` contains runtime, tooling, compatibility, session, agent, MCP, auth, telemetry, IDE, channel, and infrastructure backends
- `QwenCode.App` contains the desktop shell, Electron host, IPC bridge, and UI-facing projection layer

## Generation

Run DocFX from the repository root

```powershell
docfx docfx.json
```
